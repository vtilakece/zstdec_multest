project(
  'zstdec_multest',
  'c',
  version : '0.1.0',
  default_options : [
    'warning_level=2',
    'c_std=c11',
  ],
)

subdir('src')

# Register tests for CI 

test(
  'inspect-zstddec',
  find_program('tests/test-inspect.sh')
)

test(
  'decompress-zstddec',
  find_program('tests/test-decompress.sh')
)